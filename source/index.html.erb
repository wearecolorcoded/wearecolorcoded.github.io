---
title: Color Coded
---

<div class="row m-content">
  <div class="m-content__header-block">
    <h1 name="events">events</h1>
  </div>
  <div class="col-sm-10 col-md-10 col-lg-10 m-content__block">
    <% upcoming_events.each do |event| %>
      <%= render_partial "partials/events/event.html.erb", locals: { event: event } %>
    <% end %>
    <% past_events.each do |event| %>
      <%= render_partial "partials/events/event.html.erb", locals: { event: event } %>
    <% end %>
  </div>
  <div class="m-content__header-block">
    <h1 name="about">about</h1>
  </div>
  <div class="col-sm-10 col-md-10 col-lg-10 m-content__block">
    <div class="m-content__about-block">
      <p>
        We believe in creating safe communities where people
        of color can be themselves, connect with one another,
        and build confidence.
      </p>
      <p>
        A space that empowers us to lead at the highest levels.
      </p>
      <p>
        By nurturing engagement within our communities, we
        provide educational programming covering topics such
        as code, design, entrepreneurship, and more.
      </p>
    </div>
  </div>
</div>